spring:
  datasource:
    url: jdbc:mysql://localhost:3306/letterpick
    username: letterpick
    password: letterpickpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

newsletter:
  inbox-address:
    domain: ${NEWSLETTER_INBOX_ADDRESS_DOMAIN:inbound.letterpick.local}
  public-feed:
    collector-inbox-address: omcxewctypxo@inbound-dev.letterpicknews.com

letterpick:
  outbox:
    queue:
      trending-lifecycle-events: letterpick-local-trending-lifecycle-events
  seed:
    member:
      local-test:
        enabled: false
    newsletter:
      enabled: true
      local-test:
        enabled: true
