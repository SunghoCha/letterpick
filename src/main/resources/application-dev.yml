server:
  forward-headers-strategy: framework

spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

newsletter:
  public-feed:
    collector-inbox-address: omcxewctypxo@inbound-dev.letterpicknews.com
