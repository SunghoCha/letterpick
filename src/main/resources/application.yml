spring:
  application:
    name: letterPick
  lifecycle:
    timeout-per-shutdown-phase: 20s
  cloud:
    aws:
      region:
        static: ${LETTERPICK_AWS_REGION:ap-northeast-2}
      sqs:
        enabled: ${LETTERPICK_SQS_ENABLED:false}
        queue-not-found-strategy: FAIL
        region: ${LETTERPICK_AWS_REGION:ap-northeast-2}
  flyway:
    enabled: false
  jpa:
    properties:
      hibernate.jdbc.time_zone: UTC
  datasource:
    hikari:
      pool-name: letterpick-api
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:dummy-google-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:dummy-google-secret}
            scope: openid,profile,email
          naver:
            client-id: ${NAVER_CLIENT_ID:dummy-naver-id}
            client-secret: ${NAVER_CLIENT_SECRET:dummy-naver-secret}
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/naver"
            scope: name,email,profile_image
            client-name: Naver
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

server:
  shutdown: graceful

logging:
  pattern:
    level: "%5p [trace:%X{trace_id:-}] [span:%X{span_id:-}] [req:%X{requestId:-}]"

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health
  metrics:
    tags:
      application: ${MANAGEMENT_METRICS_TAGS_APPLICATION:${spring.application.name:letterPick}}
      environment: ${LETTERPICK_ENV:local}
      service: ${MANAGEMENT_METRICS_TAGS_SERVICE:api}

# OAuth 가입/로그인 후 redirect 대상 + CORS allowed origin.
# 배포 환경은 FRONTEND_BASE_URL 환경변수로 주입한다.
frontend:
  base-url: ${FRONTEND_BASE_URL:http://localhost:5173}

newsletter:
  inbox-address:
    domain: ${NEWSLETTER_INBOX_ADDRESS_DOMAIN:inbound.letterpick.local}
  public-feed:
    collector-inbox-address: ${NEWSLETTER_PUBLIC_FEED_COLLECTOR_INBOX_ADDRESS}

letterpick:
  public-issue:
    ranking:
      default-limit: ${LETTERPICK_PUBLIC_ISSUE_RANKING_DEFAULT_LIMIT:20}
      max-limit: ${LETTERPICK_PUBLIC_ISSUE_RANKING_MAX_LIMIT:100}
    view-count:
      snapshot-interval: ${LETTERPICK_PUBLIC_ISSUE_VIEW_COUNT_SNAPSHOT_INTERVAL:50}
      dedupe-ttl: ${LETTERPICK_PUBLIC_ISSUE_VIEW_COUNT_DEDUPE_TTL:PT30M}
      redis-key-prefix: ${LETTERPICK_PUBLIC_ISSUE_VIEW_COUNT_REDIS_KEY_PREFIX:letterpick:public-issue}
      anonymous-cookie-name: ${LETTERPICK_PUBLIC_ISSUE_VIEW_COUNT_ANONYMOUS_COOKIE_NAME:letterpick_anonymous_id}
      anonymous-cookie-max-age: ${LETTERPICK_PUBLIC_ISSUE_VIEW_COUNT_ANONYMOUS_COOKIE_MAX_AGE:P90D}
      anonymous-cookie-secure: ${LETTERPICK_PUBLIC_ISSUE_VIEW_COUNT_ANONYMOUS_COOKIE_SECURE:true}
  search:
    public-feed:
      strategy: ${LETTERPICK_PUBLIC_FEED_SEARCH_STRATEGY:fulltext}
  trending-service:
    base-url: ${LETTERPICK_TRENDING_SERVICE_BASE_URL:}
  aws:
    region: ${LETTERPICK_AWS_REGION:ap-northeast-2}
  mail:
    receive-queue: ${LETTERPICK_MAIL_RECEIVE_QUEUE:letterpick-mail-receive}
    receive-dlq: ${LETTERPICK_MAIL_RECEIVE_DLQ:${LETTERPICK_MAIL_RECEIVE_QUEUE:letterpick-mail-receive}-dlq}
    sqs-listener:
      enabled: ${LETTERPICK_MAIL_SQS_LISTENER_ENABLED:false}
  outbox:
    queue:
      trending-lifecycle-events: ${LETTERPICK_TRENDING_LIFECYCLE_EVENTS_QUEUE}
      trending-score-events: ${LETTERPICK_TRENDING_SCORE_EVENTS_QUEUE}
    publish:
      enabled: ${LETTERPICK_OUTBOX_PUBLISH_ENABLED:false}
